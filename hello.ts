import axios from 'axios';

//axios.get('https://google.com', {
//    headers: 
//});

let hello: string = 'hello2';
console.log(hello + 'a' + add(1, 2));

function add(a: number, b: number): number {
    return a + b;
}